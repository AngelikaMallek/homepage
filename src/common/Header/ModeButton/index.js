import { Wrapper, Paragraph, Button, IconButton } from "./styled";


const ModeButton = () => (
    <Wrapper>
        <Paragraph>Dark mode off</Paragraph>
        <Button>
        </Button>
    </Wrapper>
)

export default ModeButton;